#include<atcoder/all>
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <iomanip>
#include <cmath>

using namespace std;

// マクロ
#define coutYes cout << "Yes" << endl
#define coutNo cout << "No" << endl

int main(){
    string S;
    cin >> S;

    // Aを押すと桁が増える
    // Bを押すと全ての数字が上がる 9は0にループ

    // ボタンを押す回数
    // 桁を増やす桁数 + 目当ての数字にする回数

    cout << fixed << setprecision(10) << (double)cnt / 36.0 << endl;
    return 0;
}